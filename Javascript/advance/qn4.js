//How does structured clone differ from JSON serialization?

//Structured clones can copy complex objects like Maps and Sets. JSON serialization only works with basic data types. Structured clones also handle circular references. 
// It is used internally by APIs like postMessage.
//  JSON is more limited but widely supported.