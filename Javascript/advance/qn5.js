//What are TypedArrays and when would you use them?

//TypedArrays store binary data in a fixed format. 
// They are faster and more memory-efficient than normal arrays. 
// They are used in cases like graphics, audio, or handling raw data.
//  Values must match the defined type. This gives better performance.

Example: const buffer = new Uint8Array(8);